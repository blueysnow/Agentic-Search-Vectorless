# Minimal mongod config for first-time initialization only.
# No setParameter -- avoids mongotHost blocking during user creation.

replication:
  replSetName: "rs0"

net:
  bindIpAll: true
  port: 27017
